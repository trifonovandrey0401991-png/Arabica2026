# Следующие шаги после настройки JAVA_HOME

## ✅ Вы уже выполнили:
1. Установили JAVA_HOME: `$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"`
2. Обновили PATH: `$env:PATH = "$env:JAVA_HOME\bin; $env:PATH"`
3. Подтвердили, что путь существует: `Test-Path` вернул `True`

## 📋 Выполните следующие команды:

```powershell
# 1. Перейдите в папку проекта
cd C:\Users\Admin\arabica_app

# 2. Очистите кэш Gradle (это исправит ошибку с JAR файлом)
Remove-Item -Recurse -Force $env:USERPROFILE\.gradle\caches -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force $env:USERPROFILE\.gradle\daemon -ErrorAction SilentlyContinue

# 3. Очистите проект Flutter
flutter clean

# 4. Обновите зависимости
flutter pub get

# 5. Попробуйте собрать проект
flutter run
```

## ⚠️ Важно:

Если вы закроете PowerShell и откроете новый терминал, JAVA_HOME нужно будет установить снова. 

Для постоянной установки:
1. Нажмите `Win + R`, введите `sysdm.cpl`
2. Перейдите на вкладку "Дополнительно" → "Переменные среды"
3. В "Системные переменные" нажмите "Создать":
   - Имя: `JAVA_HOME`
   - Значение: `C:\Program Files\Android\Android Studio\jbr`
4. Найдите `PATH` → "Изменить" → "Создать" → добавьте: `%JAVA_HOME%\bin`
5. Перезапустите PowerShell/IDE

## 🔍 Если ошибка сохраняется:

1. Проверьте версию Java:
   ```powershell
   java -version
   ```
   Должна быть версия 11 или выше.

2. Убедитесь, что JAVA_HOME установлен:
   ```powershell
   echo $env:JAVA_HOME
   ```
   Должен показать: `C:\Program Files\Android\Android Studio\jbr`

3. Проверьте, что Java доступна:
   ```powershell
   java -version
   where.exe java
   ```



