# Инструкция по настройке функционала отзывов

## ✅ Что уже реализовано

1. ✅ Модели данных для отзывов и сообщений
2. ✅ Сервис для работы с API отзывов
3. ✅ Endpoints на сервере для работы с отзывами
4. ✅ Страница выбора типа отзыва (положительный/отрицательный)
5. ✅ Страница выбора магазина
6. ✅ Страница ввода текста отзыва
7. ✅ Страница "Отзывы покупателей" (для админа)
8. ✅ Страница "Мои диалоги" (для клиента)
9. ✅ Детальный вид диалога с возможностью ответа
10. ✅ Кнопки в главном меню

## 📋 Что нужно сделать ВАМ

### 1. Перезапустить сервер Node.js

Сервер нужно перезапустить, чтобы новые endpoints для отзывов заработали:

```bash
# Остановите текущий процесс (если запущен)
pkill -f "node index.js"

# Перейдите в папку сервера
cd /root/loyalty-proxy

# Запустите сервер заново
node index.js
```

Или если используете PM2:

```bash
pm2 restart loyalty-proxy
```

### 2. Проверить права доступа к папке для отзывов

Сервер автоматически создаст папку `/var/www/reviews`, но нужно убедиться, что у процесса Node.js есть права на запись:

```bash
# Создайте папку (если не создана)
sudo mkdir -p /var/www/reviews

# Установите права
sudo chown -R $USER:$USER /var/www/reviews
sudo chmod -R 755 /var/www/reviews
```

### 3. Настроить Nginx (опционально, если нужен доступ к API)

Если хотите, чтобы API было доступно через Nginx, добавьте в конфигурацию:

```nginx
location /api/reviews {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # CORS заголовки
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, Accept, X-Requested-With' always;
}
```

После изменений перезагрузите Nginx:

```bash
sudo nginx -t  # Проверка конфигурации
sudo systemctl reload nginx
```

### 4. Настроить Firebase для push-уведомлений (опционально, но рекомендуется)

Следуйте инструкциям в файле `FIREBASE_SETUP_INSTRUCTIONS.md`.

**Важно**: Без настройки Firebase push-уведомления работать не будут. Клиенты будут видеть новые ответы только при открытии приложения или вкладки "Мои диалоги".

### 5. Протестировать функционал

1. **Создание отзыва:**
   - Откройте приложение
   - Нажмите "Отзывы"
   - Выберите тип отзыва (положительный/отрицательный)
   - Выберите магазин
   - Введите текст отзыва
   - Нажмите "Отправить"

2. **Просмотр отзывов (админ):**
   - Нажмите "Отзывы покупателей"
   - Должен отобразиться список всех отзывов
   - Нажмите на отзыв для просмотра деталей

3. **Ответ на отзыв:**
   - Откройте детальный вид отзыва
   - Введите ответ в поле внизу
   - Нажмите кнопку отправки

4. **Просмотр диалогов (клиент):**
   - Нажмите "Мои диалоги"
   - Должны отобразиться все отзывы клиента
   - Нажмите на отзыв для просмотра диалога
   - Новые ответы должны отображаться с индикатором непрочитанных

## 🔍 Проверка работы сервера

Проверьте, что endpoints работают:

```bash
# Проверка создания отзыва
curl -X POST http://localhost:3000/api/reviews \
  -H "Content-Type: application/json" \
  -d '{
    "clientPhone": "+79991234567",
    "clientName": "Тестовый пользователь",
    "shopAddress": "г. Пятигорск, ул. Ленина, 10",
    "reviewType": "positive",
    "reviewText": "Тестовый отзыв"
  }'

# Проверка получения всех отзывов
curl http://localhost:3000/api/reviews

# Проверка получения отзывов клиента
curl "http://localhost:3000/api/reviews?phone=+79991234567"
```

## 📁 Структура файлов

Отзывы хранятся в файле `/var/www/reviews/reviews.json`.

Формат данных:
```json
[
  {
    "id": "review_1234567890_abc123",
    "createdAt": "2025-01-15T10:30:00.000Z",
    "clientPhone": "+79991234567",
    "clientName": "Иван Иванов",
    "shopAddress": "г. Пятигорск, ул. Ленина, 10",
    "reviewType": "positive",
    "reviewText": "Отличное обслуживание!",
    "messages": [
      {
        "id": "msg_1234567890_xyz789",
        "sender": "admin",
        "senderName": "Администратор",
        "text": "Спасибо за отзыв!",
        "createdAt": "2025-01-15T11:00:00.000Z",
        "isRead": false
      }
    ]
  }
]
```

## ⚠️ Важные замечания

1. **Бэкапы**: Регулярно делайте бэкапы файла `/var/www/reviews/reviews.json`
2. **Безопасность**: В будущем добавьте аутентификацию для админ-панели
3. **Масштабирование**: При большом количестве отзывов рассмотрите переход на базу данных
4. **Push-уведомления**: Без настройки Firebase уведомления работать не будут

## 🐛 Решение проблем

### Отзывы не сохраняются
- Проверьте права доступа к `/var/www/reviews`
- Проверьте логи сервера: `tail -f /var/log/loyalty-proxy.log` (если настроено)

### API не отвечает
- Убедитесь, что сервер запущен: `ps aux | grep node`
- Проверьте порт 3000: `netstat -tuln | grep 3000`

### Ошибки CORS
- Убедитесь, что CORS заголовки установлены в `index.js`
- Проверьте настройки Nginx (если используете)

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Логи сервера Node.js
2. Логи Nginx: `sudo tail -f /var/log/nginx/error.log`
3. Консоль браузера (для веб-версии)















