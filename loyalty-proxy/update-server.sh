#!/bin/bash

# Скрипт для обновления кода на сервере

echo "🔄 Обновление кода на сервере..."

# Определяем путь к проекту
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR"

# 1. Обновление кода из GitHub (если это git репозиторий)
if [ -d ".git" ]; then
    echo "1. Обновление из GitHub..."
    git pull origin main || echo "⚠️ Не удалось обновить из git (возможно, это не git репозиторий)"
else
    echo "⚠️ Это не git репозиторий, пропускаем обновление из GitHub"
    echo "   Обновите код вручную: git pull origin main"
fi

# 2. Установка/обновление зависимостей
echo ""
echo "2. Установка зависимостей..."
npm install
echo "✅ Зависимости установлены"

# 3. Создание директории для фото (если не существует)
echo ""
echo "3. Проверка директории для фото..."
mkdir -p /var/www/shift-photos
chmod 755 /var/www/shift-photos
echo "✅ Директория готова: /var/www/shift-photos"

# 4. Перезапуск сервера
echo ""
echo "4. Перезапуск сервера..."
if command -v pm2 &> /dev/null; then
    echo "   Используется PM2..."
    pm2 restart loyalty-proxy || pm2 start index.js --name loyalty-proxy
    echo "✅ Сервер перезапущен через PM2"
else
    echo "⚠️ PM2 не найден"
    echo "   Перезапустите сервер вручную:"
    echo "   node index.js"
fi

echo ""
echo "✅ Обновление завершено!"
echo ""
echo "Проверьте логи:"
echo "   pm2 logs loyalty-proxy"







