# Инструкция по исправлению Firebase

## ✅ Что было исправлено

1. **Улучшена логика инициализации Firebase:**
   - Увеличена задержка перед инициализацией (2000ms вместо 1000ms)
   - Добавлена повторная попытка инициализации при ошибке
   - Увеличено количество попыток проверки доступности App (30 вместо 20)
   - Добавлено подробное логирование ошибок
   - Приложение продолжает работу даже если Firebase не инициализирован

2. **Создан скрипт для получения SHA-сертификатов:**
   - Файл: `get-sha-certificates.ps1`
   - Автоматически находит Java
   - Получает SHA-1 и SHA-256
   - Копирует в буфер обмена

## 📋 Что нужно сделать

### Шаг 1: Получить SHA-сертификаты

1. Скопируйте файл `get-sha-certificates.ps1` на ваш компьютер:
   ```powershell
   # Файл находится в корне проекта на сервере
   # Скопируйте его в: C:\Users\Admin\arabica2026\
   ```

2. Выполните скрипт:
   ```powershell
   cd C:\Users\Admin\arabica2026
   .\get-sha-certificates.ps1
   ```

3. Скрипт автоматически:
   - Найдет Java
   - Получит SHA-1 и SHA-256
   - Скопирует их в буфер обмена

### Шаг 2: Добавить SHA-сертификаты в Firebase

1. Откройте Firebase Console:
   https://console.firebase.google.com/

2. Выберите проект: `arabica2027-6d78d`

3. Перейдите:
   **Project settings** → **General** → **Your apps** → **Android app (Arabica2027)**

4. Найдите раздел **"SHA certificate fingerprints"**

5. Нажмите **"Add fingerprint"** и добавьте:
   - SHA-1 (из вывода скрипта)
   - SHA-256 (из вывода скрипта)

6. Сохраните изменения

### Шаг 3: Обновить код на сервере

Если вы работаете с кодом на сервере, выполните:

```bash
cd /root/arabica_app
git pull  # если используете git
# или просто скопируйте обновленные файлы
```

### Шаг 4: Перезапустить приложение

```powershell
cd C:\Users\Admin\arabica2026
flutter clean
flutter pub get
flutter run
```

## 🔍 Проверка работы

После перезапуска приложения проверьте логи. Должны появиться:

```
🔵 Начало инициализации Firebase Core...
🔵 Платформа: android
🔵 Ожидание обработки google-services.json плагином...
🔵 Вызов Firebase.initializeApp()...
✅ Firebase.initializeApp() завершен успешно
🔵 Проверка доступности Firebase App...
✅ Firebase App доступен: [DEFAULT] (попытка 1)
✅ Firebase инициализирован и готов к использованию
✅ Firebase App найден перед созданием Messaging: [DEFAULT]
✅ Экземпляр FirebaseMessaging создан
✅ Пользователь разрешил уведомления
📱 FCM Token получен: ...
✅ FCM токен сохранен на сервере
```

## ⚠️ Если проблема сохраняется

1. **Проверьте, что `google-services.json` находится в правильном месте:**
   - Должен быть: `android/app/google-services.json`
   - НЕ должен быть: `android/google-services.json`

2. **Проверьте, что SHA-сертификаты добавлены в Firebase:**
   - Откройте Firebase Console
   - Проверьте раздел "SHA certificate fingerprints"
   - Должны быть добавлены SHA-1 и SHA-256

3. **Проверьте логи при запуске:**
   - Ищите ошибки, связанные с Firebase
   - Проверьте, что используется реальный Firebase Core, а не stub

4. **Попробуйте полностью пересобрать проект:**
   ```powershell
   flutter clean
   cd android
   .\gradlew clean
   cd ..
   flutter pub get
   flutter run
   ```

## 📝 Дополнительная информация

- **Project ID:** `arabica2027-6d78d`
- **Project Number:** `974766337240`
- **Android Package:** `com.example.arabica_app`
- **iOS Bundle ID:** `com.example.arabicaApp`

## 🆘 Если нужна помощь

Пришлите:
1. Полные логи запуска приложения
2. Вывод скрипта `get-sha-certificates.ps1`
3. Скриншот раздела "SHA certificate fingerprints" в Firebase Console



