# Пошаговая инструкция по настройке Google Apps Script

## Проблема: "Не удалось найти функцию скрипта: doGet"

Эта ошибка означает, что скрипт не был правильно сохранен или развернут.

## Решение:

### Шаг 1: Откройте Google Apps Script

1. Откройте Google Sheets: https://docs.google.com/spreadsheets/d/1n7E3sph8x_FanomlEuEeG5a0OMWSz9UXNlIjXAr19MU
2. Перейдите в **Расширения** → **Apps Script**
3. Откроется редактор Apps Script

### Шаг 2: Удалите весь старый код

1. В редакторе Apps Script выделите весь код (Ctrl+A)
2. Удалите его (Delete)

### Шаг 3: Вставьте новый код

1. Откройте файл `google_apps_script.js` в вашем проекте
2. Скопируйте **весь код** из файла (Ctrl+A, Ctrl+C)
3. Вставьте в редактор Apps Script (Ctrl+V)

### Шаг 4: Сохраните проект

1. Нажмите **Ctrl+S** или кнопку **Сохранить** (💾)
2. Дайте проекту имя (например, "Registration API")
3. Убедитесь, что код сохранен

### Шаг 5: Проверьте, что функции существуют

В редакторе должны быть видны две функции:
- `doPost(e)` - для POST запросов
- `doGet(e)` - для GET запросов

Если функций нет, значит код не был вставлен правильно.

### Шаг 6: Создайте новое развертывание

1. Нажмите **Развернуть** → **Новое развертывание**
2. Нажмите на иконку **шестеренки** (⚙️) рядом с "Выберите тип"
3. Выберите **Веб-приложение**
4. Заполните настройки:
   - **Описание**: "Registration API"
   - **Выполнять от имени**: **"Меня"** (ваш email)
   - **У кого есть доступ**: **"Все"**
5. Нажмите **Развернуть**
6. При первом развертывании появится запрос на авторизацию:
   - Нажмите **Разрешить доступ**
   - Выберите свой аккаунт Google
   - Нажмите **Дополнительно** → **Перейти к [название проекта] (небезопасно)**
   - Нажмите **Разрешить**

### Шаг 7: Скопируйте URL веб-приложения

1. После развертывания появится окно с URL
2. Скопируйте **URL веб-приложения**
3. Он будет выглядеть примерно так:
   ```
   https://script.google.com/macros/s/AKfycby.../exec
   ```

### Шаг 8: Обновите URL в коде приложения

1. Откройте файл `lib/registration_service.dart`
2. Найдите строку:
   ```dart
   static const String _scriptUrl =
       'https://script.google.com/macros/s/AKfycby2OrfeRnPzi6maPHM4ijqiG7B_s2NqqXrDTJegiUzzxYXLvMEFqDBGWJ2Sj9dNdIMj/exec';
   ```
3. Замените URL на новый (если он изменился)

### Шаг 9: Тестирование

#### Тест 1: Проверка через браузер (GET запрос)

Откройте в браузере (замените URL на ваш):
```
https://script.google.com/macros/s/ВАШ_URL/exec?name=Тест&phone=%2B79001234567
```

Если данные появились в таблице - скрипт работает!

#### Тест 2: Проверка через приложение

1. Запустите приложение
2. Зарегистрируйтесь снова
3. Проверьте консоль - должны быть сообщения:
   - `📤 Ответ от Google Apps Script: 200`
   - `📄 Тело ответа: {...}`
   - `📋 Данные ответа: {success: true, ...}`
4. Проверьте таблицу - данные должны появиться в листе "Лист11"

## Важно:

- После каждого изменения кода в Apps Script нужно создавать **новое развертывание**
- Или обновлять существующее: **Развернуть** → **Управление развертываниями** → **Редактировать** → **Новая версия** → **Развернуть**

## Если проблема сохраняется:

1. Проверьте логи выполнения:
   - В Apps Script выберите функцию `doPost` или `doGet`
   - Нажмите **Выполнить** (▶️)
   - Проверьте логи внизу

2. Проверьте права доступа:
   - **Развернуть** → **Управление развертываниями**
   - Убедитесь, что "У кого есть доступ" = **"Все"**

3. Убедитесь, что лист "Лист11" существует в таблице

