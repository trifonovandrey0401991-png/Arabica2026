# Проверка системы ролей - Пошаговая инструкция

## ✅ Все файлы на GitHub проверены и присутствуют!

Я проверил репозиторий - все файлы на месте:
- ✅ `lib/role_test_page.dart` - существует
- ✅ `lib/user_role_model.dart` - существует  
- ✅ `lib/user_role_service.dart` - существует
- ✅ `lib/main_menu_page.dart` - обновлен с кнопкой "Тест ролей"
- ✅ Импорты добавлены
- ✅ Метод `_getMenuItems()` вызывается правильно

## 🔧 Что нужно сделать на вашем компьютере:

### Шаг 1: Убедитесь, что вы в правильной директории

```bash
cd arabica2026
# или
cd arabica_app
```

### Шаг 2: Проверьте статус git

```bash
git status
```

Должно показать, что вы на ветке `main` и синхронизированы с `origin/main`.

### Шаг 3: Обновите код с GitHub

```bash
git fetch origin
git pull origin main
```

### Шаг 4: Запустите скрипт проверки

```bash
chmod +x CHECK_ROLE_FILES.sh
./CHECK_ROLE_FILES.sh
```

Скрипт проверит:
- Наличие всех файлов
- Импорты в main_menu_page.dart
- Наличие кнопки "Тест ролей"
- Вызов метода _getMenuItems()

### Шаг 5: Полностью очистите и пересоберите проект

```bash
flutter clean
flutter pub get
flutter pub upgrade
```

### Шаг 6: Удалите старую версию приложения

**На Android:**
```bash
adb uninstall com.example.arabica_app
```

**Или вручную:**
- Настройки → Приложения → arabica_app → Удалить

### Шаг 7: Пересоберите и установите заново

```bash
flutter run
```

## 🔍 Ручная проверка файлов:

### 1. Проверьте наличие файла role_test_page.dart:

```bash
ls -la lib/role_test_page.dart
```

Должен показать файл.

### 2. Проверьте импорт в main_menu_page.dart:

```bash
grep "role_test_page" lib/main_menu_page.dart
```

Должна быть строка: `import 'role_test_page.dart';`

### 3. Проверьте кнопку "Тест ролей":

```bash
grep -n "Тест ролей" lib/main_menu_page.dart
```

Должны быть строки с кнопкой (около строки 331-337).

### 4. Проверьте метод _getMenuItems():

```bash
grep -n "_getMenuItems" lib/main_menu_page.dart
```

Должны быть:
- Строка ~148: `children: _getMenuItems(),`
- Строка ~159: `List<Widget> _getMenuItems() {`

## 🐛 Если кнопка всё ещё не появляется:

### Вариант 1: Проблема с кэшем Flutter

```bash
flutter clean
rm -rf build/
rm -rf .dart_tool/
flutter pub get
flutter run
```

### Вариант 2: Проблема с горячей перезагрузкой

Полностью закройте приложение и запустите заново:
```bash
# Остановите текущий процесс (Ctrl+C)
flutter run
```

### Вариант 3: Проверьте консоль на ошибки

При запуске `flutter run` проверьте:
- Нет ли ошибок компиляции
- Нет ли ошибок импорта
- Нет ли предупреждений о missing files

### Вариант 4: Проверьте версию кода

```bash
git log --oneline -1
```

Должен быть коммит: `90467cf Update Google Apps Script URL in proxy server`

Если нет, выполните:
```bash
git pull origin main
```

### Вариант 5: Проверьте, что файлы действительно обновлены

```bash
git diff HEAD lib/main_menu_page.dart
```

Если есть изменения, значит файл не обновлен. Выполните:
```bash
git checkout lib/main_menu_page.dart
```

## 📱 Проверка в приложении:

1. Запустите приложение
2. Откройте главное меню
3. Прокрутите вниз - кнопка "Тест ролей" должна быть в конце списка
4. Иконка: 🔬 (колба/наука)

## ⚠️ Важно:

- Кнопка "Тест ролей" должна быть видна **ВСЕМ** пользователям (независимо от роли)
- Она находится в **конце списка** всех кнопок
- Если вы не видите её, возможно нужно прокрутить меню вниз

## 📞 Если ничего не помогло:

Выполните и пришлите результат:

```bash
./CHECK_ROLE_FILES.sh
git log --oneline -5
git status
flutter doctor
```

Это поможет найти проблему.







