# 🚀 Быстрый запуск через PowerShell

## ⚡ Самый простой способ

### 1. Измените URL в конфигурации

Откройте файл `lib/google_script_config.dart` и измените на:
```dart
const String googleScriptUrl = 'http://127.0.0.1:3000';
```

### 2. Откройте ДВА окна PowerShell

### 3. В ПЕРВОМ окне выполните:

```powershell
cd arabica_app\loyalty-proxy
npm install
node index.js
```

**Оставьте это окно открытым!** Должно появиться: `Proxy listening on port 3000`

### 4. Во ВТОРОМ окне выполните:

```powershell
cd arabica_app
flutter run -d chrome
```

Готово! Приложение запустится в браузере.

---

## 📝 Подробные команды

### Терминал 1 - Прокси-сервер:
```powershell
# Перейти в папку прокси
cd arabica_app\loyalty-proxy

# Установить зависимости (только первый раз)
npm install

# Запустить прокси
node index.js
```

### Терминал 2 - Flutter приложение:
```powershell
# Перейти в папку проекта
cd arabica_app

# Запустить приложение
flutter run -d chrome
```

---

## 🛑 Остановка

- В Терминале 2: нажмите `q` или `Ctrl+C`
- В Терминале 1: нажмите `Ctrl+C`

---

## ❓ Проблемы?

См. файл `POWERSHELL_COMMANDS.md` для подробной инструкции.






