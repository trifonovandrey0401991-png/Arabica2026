# Исправление проблемы с git status

## Проблема: git говорит о изменениях, но `git diff` ничего не показывает

Это может быть из-за:
- Различий в окончаниях строк (CRLF vs LF)
- Проблем с правами доступа
- Кэша git

## Решение:

### Шаг 1: Проверьте статус файла

```powershell
git status lib/main.dart
```

### Шаг 2: Принудительно обновите файл

```powershell
# Удалите файл из индекса
git rm --cached lib/main.dart

# Восстановите файл из HEAD
git checkout HEAD -- lib/main.dart

# Или принудительно обновите
git checkout -f lib/main.dart
```

### Шаг 3: Если не помогло, используйте stash

```powershell
# Сохранить все изменения
git stash

# Обновить код
git pull origin main

# Если нужно вернуть изменения
git stash pop
```

### Шаг 4: Если все еще не работает, принудительно обновите

```powershell
# Принудительно обновить файл из удаленного репозитория
git fetch origin
git reset --hard origin/main
```

**⚠️ Внимание:** `git reset --hard` удалит все локальные изменения!

### Шаг 5: Альтернативный способ - обновить только один файл

```powershell
# Обновить только lib/main.dart из удаленного репозитория
git fetch origin
git checkout origin/main -- lib/main.dart
git add lib/main.dart
git commit -m "Update main.dart from remote"
```

## После обновления:

1. Проверьте, что код обновлен:
```powershell
Select-String -Path lib\main_menu_page.dart -Pattern "🔵 _getMenuItems"
```

2. Пересоберите приложение:
```powershell
flutter clean
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
flutter pub get
flutter run
```












