# Отчет по ошибкам тестирования приложения
**Дата:** 31 декабря 2025
**Продолжительность тестирования:** ~5 минут
**Статус:** Обнаружены критические ошибки

---

## Краткое резюме

Во время тестирования приложения было обнаружено **2 критических проблемы**:
1. Ошибка загрузки эталонных фотографий
2. Проблема подключения к серверу по SSH

---

## Детальный анализ ошибок

### ❌ КРИТИЧЕСКАЯ ОШИБКА #1: Загрузка эталонного фото

**Описание:**
Приложение не может загрузить эталонное фото для вопроса смены.

**Логи:**
```
📤 Загрузка эталонного фото для вопроса: shift_question_1765713490571,
   магазин: Ессентуки , ул пятигорская 149/1 (Золотушка)
❌ ❌ Ошибка загрузки эталонного фото
   Ошибка: Unsupported operation: MultipartFile is only supported where dart:io is available.
```

**Причина:**
Ошибка указывает на попытку использовать `MultipartFile` в окружении, где `dart:io` недоступен. Это может происходить при попытке выполнить код, предназначенный для платформ с файловой системой (Android/iOS), в веб-окружении или при неправильной конфигурации платформозависимого кода.

**Затронутый функционал:**
- Модуль сдачи смены (shift handover)
- Загрузка эталонных фотографий для вопросов смены
- Работа с магазином "Ессентуки, ул пятигорская 149/1 (Золотушка)"

**Приоритет:** 🔴 ВЫСОКИЙ
**Влияние:** Блокирует функциональность сдачи смены с эталонными фотографиями

---

### ⚠️ ПРОБЛЕМА #2: SSH подключение к серверу

**Описание:**
Невозможно установить SSH соединение с сервером для получения серверных логов.

**Логи:**
```
ssh: connect to host 192.168.1.100 port 22: Connection timed out
```

**Причина:**
- Сервер может быть недоступен
- Проблемы с сетевым подключением
- Firewall блокирует порт 22
- Неверный IP адрес сервера

**Приоритет:** ⚠️ СРЕДНИЙ
**Влияние:** Невозможность мониторинга серверных логов (не влияет на работу приложения)

---

## Успешные операции

✅ **Загрузка магазинов:**
- Успешно загружено 8 магазинов с сервера
- API endpoint `/api/shops` работает корректно
- Кэширование магазинов функционирует

```
📥 GET /api/shops
✅ Loaded 8 items from /api/shops
✅ Загружено магазинов: 8
```

---

## Рекомендации по исправлению

### Для ошибки #1 (MultipartFile)

**Необходимо проверить:**
1. Условную компиляцию для разных платформ
2. Использование правильных импортов (`dart:io` vs `dart:html`)
3. Платформозависимую логику загрузки файлов
4. Конфигурацию для Android/iOS vs Web

**Файлы для проверки:**
- Модули работы с эталонными фотографиями
- Сервисы загрузки файлов
- API клиенты, использующие MultipartFile

### Для проблемы #2 (SSH подключение)

**Необходимо проверить:**
1. Доступность сервера (ping 192.168.1.100)
2. Статус SSH службы на сервере
3. Настройки firewall
4. Корректность IP адреса

---

## Статистика

| Параметр | Значение |
|----------|----------|
| Всего обнаружено ошибок | 2 |
| Критических | 1 |
| Предупреждений | 1 |
| Успешных операций | 3 |
| Затронутые модули | Shift handover, File upload |

---

## Приоритет устранения

1. **Первоочередная задача:** Исправить ошибку загрузки эталонных фото (MultipartFile)
2. **Вторая задача:** Проверить доступность сервера и SSH подключение

---

**Отчет сгенерирован автоматически**
**Основание:** Логи тестирования приложения Arabica 2026
